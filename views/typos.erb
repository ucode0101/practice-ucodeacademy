<div class="practice-page">
  <a href="/" class="back-link">← Back to exercises</a>
  <h1 class="practice-title">Typos</h1>
  <div class="practice-banner"><ul>
    <li>Read the paragraph text and compare to the expected value</li>
    <li>Handle random variation: the page shows either the correct text or a typo</li>
    <li>Use regex or <code>assertThat(text).isIn(...)</code> to handle both possible strings</li>
  </ul></div>
<div class='example'>
  
  <p>This example demonstrates a typo being introduced. It does it randomly on each page load.<p>
  <%= @copy %>
</div>
</div>

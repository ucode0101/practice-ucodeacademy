<div class="practice-page">
  <a href="/" class="back-link">← Back to exercises</a>
  <h1 class="practice-title">Inputs</h1>
  <div class="practice-banner"><ul>
    <li>Type a number using <code>sendKeys()</code> and verify the value with <code>getAttribute("value")</code></li>
    <li>Clear the field with <code>clear()</code> then type a new value</li>
    <li>Use arrow keys to increment/decrement the number input</li>
    <li>Verify the browser rejects non-numeric input for a number field</li>
  </ul></div>

  <p style="color:#64748b; font-size:0.9rem; margin-bottom:0.3rem;">Number</p>
  <input type="number" id="number-input" style="padding:0.4rem 0.6rem; border:1px solid #cbd5e1; border-radius:6px; width:200px;">
</div>
